(async()=>{try{const n=navigator,s=screen,ua=n.userAgent;let browser='Unknown';if(/Edg\//.test(ua))browser='Edge';else if(/Chrome\//.test(ua))browser='Chrome';else if(/Firefox\//.test(ua))browser='Firefox';else if(/Safari\//.test(ua))browser='Safari';const raw=[ua,n.platform,n.language,s.width,s.height,s.colorDepth,Intl.DateTimeFormat().resolvedOptions().timeZone].join('|'),bytes=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(raw)),fingerprint=[...new Uint8Array(bytes)].map(x=>x.toString(16).padStart(2,'0')).join('');let session_id=sessionStorage.getItem('lwo-session');if(!session_id){session_id=crypto.randomUUID();sessionStorage.setItem('lwo-session',session_id)}await fetch('/_lwo/client-context',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({fingerprint,session_id,user_agent:ua,browser,platform:n.userAgentData?.platform||n.platform,language:n.language,screen:`${s.width}x${s.height}@${s.colorDepth}`,timezone:Intl.DateTimeFormat().resolvedOptions().timeZone}),keepalive:true})}catch{}})();
